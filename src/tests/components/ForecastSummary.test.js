import React from "react";
import { render } from "@testing-library/react";
import ForecastSummary from "../../components/ForecastSummary";

describe("ForecastSummary", () => {
  it("renders the forecast summary for an individual day"),
    () => {
      const {} = render();
    };
});
